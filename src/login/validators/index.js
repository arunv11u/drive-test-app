import { validateLoginInputs } from "./login.validator"; // Importing the validator function for login inputs

// Exporting the validator function for external use
module.exports = {
	validateLoginInputs
};