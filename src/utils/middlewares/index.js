/**
 * Importing the getCookiesMiddleware function from the "./get-cookies.middleware" module.
 */
const { getCookiesMiddleware } = require("./get-cookies.middleware");

/**
 * Exporting the getCookiesMiddleware function for use in other modules.
 */
module.exports = {
	getCookiesMiddleware
};